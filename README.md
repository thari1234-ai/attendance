# attendance
# Face Attendance System

A simple web-based attendance system using Streamlit.

- Enter name & roll number.
- Capture photo via the browser camera.
- Attendance is saved along with photo, time, and a random motivational quote.
- Displays all previous attendance records.

## How to run locally

```bash
pip install -r requirements.txt
streamlit run appp.py
